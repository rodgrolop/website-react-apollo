import { makeStyles } from '@material-ui/core/styles'
export const useStyles = makeStyles(theme => ({
  gridContainer: { width: '100%', margin: '10px 0' },
  gridInput: { width: 'calc(100% - 32px)' },
  input: { width: '100%' }
}))
